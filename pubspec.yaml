name: fetchly
description: Fetchly is a simple way to use Dio in Flutter.
version: 0.0.1
homepage: https://github.com/ashtav/fetchly

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  dio: ^5.3.2
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter: null